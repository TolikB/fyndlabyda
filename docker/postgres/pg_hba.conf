# Local bootstrap/health remains password-authenticated; all TCP clients require TLS,
# SCRAM, and a CA-verified client certificate whose CN matches the database role.
local   all             all                                     scram-sha-256
hostnossl all           all             0.0.0.0/0               reject
hostnossl all           all             ::/0                    reject
hostssl all             all             0.0.0.0/0               scram-sha-256 clientcert=verify-full
hostssl all             all             ::/0                    scram-sha-256 clientcert=verify-full